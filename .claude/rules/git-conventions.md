# Git Conventions (Project Override)

## Default Branch

The default branch for this repository is `korte` (not `main`).

- Use `korte` as the base branch for all PRs
- Feature/fix branches should be created from `korte`
- NEVER force push to `korte`
- NEVER commit directly to `korte` — always go through a PR
