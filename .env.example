# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Service role key — server-side only (API routes). Never exposed to browser.
# No  prefix = Next.js keeps it server-side.
SUPABASE_SERVICE_ROLE_KEY=

# JWT secret for session cookies — use a strong random string in production
JWT_SECRET=
