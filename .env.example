# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — server-side only (API routes). Never exposed to browser.
# No NEXT_PUBLIC_ prefix = Next.js keeps it server-side.
SUPABASE_SERVICE_ROLE_KEY=
